# Magic Rampage — AArch64 PortMaster port

English | [Português](#português)

## English

This repository contains the open-source Linux adapter for compatible owner
data from **Magic Rampage 7.8.2 and 7.8.7**. It runs the owner's original
Android AArch64 game library through the native lifecycle; proprietary data is
never redistributed.

### Architecture and framework

The visible launcher is generated by `nxbootstrap 0.6.14` through
`nxgenerator 0.2.8`. It provides the
PortMaster hand-off, single-instance lock, safe logging, NXExtract invocation,
controller environment and supervised shutdown. The adapter keeps JNI,
lifecycle, GLES, SDL_mixer audio, input translation and persistence at the
game-specific boundary. `FRAMEWORK-PIN.json` records immutable versions and
SHA-256 pins for the complete NextOS framework snapshot.

After NXExtract has completed and the owner payload has passed its gate,
`nxsplash 0.1.2` displays the mandatory bilingual **RETRO ELITE** hand-off for
five seconds. It runs before the adapter, private Android libraries and native
game lifecycle, has no per-port skip switch, and fails closed if its pinned
binary is missing or altered.

NXExtract 1.2.9 searches `gamedata/` and the port directory, identifies the
package independently of its filename, selects only `arm64-v8a`, validates the
container bounds plus exact critical native payloads, and commits them
transactionally. Its pinned low-GLIBC bilingual graphical UI uses the approved
SDL/framebuffer renderer and must attest visible output before source scanning; it cannot
silently disappear or die during setup. No ARMv7 binary is built or shipped.

### Physical support

The established playable baseline was physically verified from release 1.1.0
on 2026-08-14:

- NextOS, Mali-450, AArch64: native Mali/GLES 2.0 video and playable runtime.
- R36S ArkOS, Mali-G31, AArch64: KMSDRM/GLES 3.2 video and playable runtime.

On both families, NXExtract accepted the documented 7.8.2 reference, the
mandatory five-second hand-off completed, and the original native lifecycle
reached the game runtime. Music/SFX, controller and gameplay remain covered by
the established playable baseline. The 7.8.7 APKM passed host plan, complete
extraction and verification; its exact physical ArkOS test is still pending.

The adapter first preserves the firmware/PortMaster SDL provider selection. On
the proven R36S firmware only, a failed SDL window creation permits one narrow
retry with the real combined Mali EGL/GLES DSO; no video driver is forced
globally.

### Controls

| Action | Controls |
| --- | --- |
| Move | D-pad or left stick |
| Jump / confirm | A |
| Up | D-pad up or left stick |
| Attack | X, Y or R1 |
| Inventory | L1 |
| Shop / secondary action | L2 |
| Accept | A or Start |
| Back / cancel | B or Select |
| Exit port | Select + Start |

### Owner data and installation

See [INSTALLATION.md](INSTALLATION.md). The documented legal references cover
Magic Rampage 7.8.2 and 7.8.7 for `com.asanteegames.magicrampage`, with exact
container sizes and SHA-256 values. Compatible packaging variants are gated by
package, ABI, container bounds and exact critical payload hashes. Saves remain
inside the port's `user/` directories.

### Build

Set `MAGICRAMPAGE_SDK` to an AArch64 SDL2/SDL2_mixer/libzip development sysroot
and run `./build-glibc230.sh`. The Docker recipe uses a pinned Debian Buster
base; every produced Linux ELF must require at most `GLIBC_2.30`. The release
binary is named `magicrampage-nextos` and currently requires `GLIBC_2.17`.

### Source map and licenses

- `src/`: game adapter, loader, JNI, GLES, audio and controls.
- `project/`: nxgenerator and NXExtract source manifests.
- `magicrampage/`: generated runtime contract and public documentation.
- `build-glibc230.sh`: low-glibc AArch64 build.
- `package/build-package.sh`: deterministic public-quality release gates.

Project code is GPL-3.0-only. Framework-derived portions and their upstream
attributions are recorded in `NOTICE.md`; the game remains property of its
owner.

## Português

Este repositório contém o adapter Linux de código aberto para dados compatíveis
do dono de **Magic Rampage 7.8.2 e 7.8.7**. Ele executa a biblioteca Android
AArch64 original seguindo o lifecycle nativo; dados proprietários nunca são
redistribuídos.

### Arquitetura e framework

O launcher visível foi gerado pelo `nxbootstrap 0.6.14` por meio do
`nxgenerator 0.2.8`. Ele faz a integração
PortMaster, trava de instância única, log seguro, chamada do NXExtract, ambiente
de controles e encerramento supervisionado. JNI, lifecycle, GLES, áudio por
SDL_mixer, tradução dos controles e persistência ficam no adapter específico do
jogo. `FRAMEWORK-PIN.json` fixa versões imutáveis e SHA-256 do snapshot completo
do framework NextOS.

Depois de o NXExtract terminar e os dados do dono passarem pelo gate, o
`nxsplash 0.1.2` exibe por cinco segundos a transição bilíngue obrigatória
**RETRO ELITE**. Ela roda antes do adapter, das bibliotecas Android privadas e do
lifecycle nativo, não possui opção de remoção por port e falha de forma fechada
se o binário fixado estiver ausente ou adulterado.

O NXExtract 1.2.9 procura em `gamedata/` e na pasta do port, reconhece o pacote
sem depender do nome do arquivo, escolhe somente `arm64-v8a`, valida limites do
container e os payloads nativos críticos exatos e faz commit transacional. Sua
UI gráfica bilíngue pinada e de glibc baixa usa o renderer SDL/framebuffer aprovado e precisa atestar
saída visível antes do scan; ela não pode sumir silenciosamente nem morrer
durante o setup. Nenhum binário ARMv7 é criado ou incluído.

### Suporte físico

O baseline jogável estabelecido foi verificado fisicamente na release 1.1.0 em
14/08/2026:

- NextOS, Mali-450, AArch64: vídeo Mali/GLES 2.0 nativo e runtime jogável.
- R36S ArkOS, Mali-G31, AArch64: vídeo KMSDRM/GLES 3.2 e runtime jogável.

Nas duas famílias, o NXExtract aceitou a referência 7.8.2 documentada, a
transição obrigatória de cinco segundos terminou e o lifecycle nativo original
chegou ao runtime do jogo. Música/efeitos, controle e gameplay continuam
cobertos pelo baseline jogável. O APKM 7.8.7 passou por plan, extração completa
e verificação no host; ainda falta o teste físico exato no ArkOS.

O adapter preserva primeiro a escolha SDL do firmware/PortMaster. Somente no
firmware R36S comprovado, uma falha real ao criar a janela permite uma única
tentativa estreita com a DSO Mali que fornece EGL e GLES juntas; nenhum driver
de vídeo é forçado globalmente.

### Controles

| Ação | Controles |
| --- | --- |
| Mover | Direcional ou analógico esquerdo |
| Pular / confirmar | A |
| Cima | Direcional para cima ou analógico esquerdo |
| Atacar | X, Y ou R1 |
| Inventário | L1 |
| Loja / ação secundária | L2 |
| Confirmar | A ou Start |
| Voltar / cancelar | B ou Select |
| Sair do port | Select + Start |

### Dados do dono e instalação

Veja [INSTALLATION.md](INSTALLATION.md). As referências legais documentadas
cobrem Magic Rampage 7.8.2 e 7.8.7, pacote
`com.asanteegames.magicrampage`, com tamanhos e SHA-256 exatos. Variantes
compatíveis passam por package, ABI, limites do container e hashes exatos dos
payloads críticos. Os saves permanecem nas pastas `user/` do port.

### Compilação

Defina `MAGICRAMPAGE_SDK` para um sysroot de desenvolvimento AArch64 com
SDL2/SDL2_mixer/libzip e rode `./build-glibc230.sh`. A receita Docker usa uma
base Debian Buster fixada; todo ELF Linux produzido deve exigir no máximo
`GLIBC_2.30`. O binário da release se chama `magicrampage-nextos` e atualmente
exige `GLIBC_2.17`.

### Mapa de fontes e licenças

- `src/`: adapter, loader, JNI, GLES, áudio e controles.
- `project/`: manifestos-fonte do nxgenerator e NXExtract.
- `magicrampage/`: contrato gerado de runtime e documentação pública.
- `build-glibc230.sh`: build AArch64 com glibc baixa.
- `package/build-package.sh`: gates determinísticos de qualidade pública.

O código do projeto usa GPL-3.0-only. Partes derivadas do framework e atribuições
upstream estão em `NOTICE.md`; o jogo continua propriedade de seu titular.
